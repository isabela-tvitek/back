datasource db {
  provider = "sqlite"
}

generator client {
  provider      = "prisma-client"
  output        = "../src/generated/prisma"
  moduleFormat  = "cjs"
}

model Tasks {
  id Int @id @default(autoincrement())
  title String @unique
  description String?
}